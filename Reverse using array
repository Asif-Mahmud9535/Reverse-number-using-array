#include<stdio.h>
main()
{
    int n,i,m,ar[10],count=0;

    scanf("%d",&n);

    for(i=0;i<10&&n!=0;i++)
    {
        m = n%10;
        ar[i]=m;
        count++;
        n = n/10;
    }
    for(i=0;i<count;i++)
    {
        printf("%d\t",ar[i]);
    }

}
